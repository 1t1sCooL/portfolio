export const body = `
Прогнал Lamoda через Lighthouse. Цифры — публичные объективные замеры. Сразу важная оговорка: значения здесь экстремальные, и это может отражать временное состояние (баннер, троттлинг, сбой на момент замера). Сигнал «очень тяжёлый клиент» устойчивый, но конкретные числа стоит перепроверять свежим прогоном.

## Метрики

| Метрика | Mobile | Desktop |
| --- | --- | --- |
| Performance | 🔴 0 | 🔴 0 |
| LCP | 66.3 с | — |
| FCP | 13.5 с | — |
| TBT | 2.7 с | — |
| CLS | 0.236 | 1.122 |
| Best Practices | 38.5 | 73 |
| Security-заголовки | 0 из 6 | — |

## Что не так

- **Performance 0** — страница настолько тяжёлая, что на троттлинге Lighthouse почти не догружает контент: LCP 66 секунд, FCP 13.5 с. Гигантский JavaScript-клиент маркетплейса на старте.
- **CLS 1.122 на десктопе** — контент сильно скачет при загрузке: огромное совокупное смещение макета, пользователь промахивается по кнопкам.
- Ни одного security-заголовка.

## Что починить первым

Агрессивный code-splitting и отложенная загрузка, серверный рендер первого экрана, резерв места под изображения и баннеры (убрать CLS). Цель — хотя бы FCP < 3 с и стабильная вёрстка.

**Вывод:** крупный fashion-маркетплейс с признаками очень тяжёлого клиента — длинный LCP и прыгающая вёрстка на загрузке.

---

*Замер: публичный Lighthouse (mobile + desktop), 18.06.2026. Значения экстремальные и могут быть точкой во времени — проверяйте свежим прогоном.*

Это разбор из серии [«6 известных сайтов через Lighthouse»](/blog/mini-audity-6-sajtov). Хотите корректный разбор своего сайта с планом фиксов? — [аудит и ускорение сайта](/audit).
`;
